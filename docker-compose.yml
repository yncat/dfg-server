version: "3"
services:
  dfg-server:
    build:
      context: .
    command: "npm start"
    environment:
      SLACK_WEBHOOK_URL: ${SLACK_WEBHOOK_URL}
    image: dfg-server:latest
    ports:
      - "2567:2567"
